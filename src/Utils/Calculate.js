const add = (x, y) => {
    return x + y;
}

const multiply = (x, y) => {
    return x * y;
}

const divideTheFirstNumberByTheSecondNumber = (x, y) => {
    return x / y;
}

export { add, multiply, divideTheFirstNumberByTheSecondNumber }